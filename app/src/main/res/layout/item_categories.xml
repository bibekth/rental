<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Square box with border using FrameLayout -->
    <FrameLayout
        android:id="@+id/squareBox"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_margin="0.5dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@drawable/border_square_box">

        <!-- Centered RoundedImageView inside the square box -->
        <com.makeramen.roundedimageview.RoundedImageView
            android:id="@+id/catimg"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_gravity="center"
            android:src="@drawable/placeholder"
            app:riv_oval="true"
            android:scaleType="centerCrop"
            android:padding="1dp"
            app:riv_mutate_background="true"/>
    </FrameLayout>

    <TextView
        android:id="@+id/cattxt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:gravity="center"
        android:textColor="@color/text"
        android:text="TextView"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/squareBox"/>
</androidx.constraintlayout.widget.ConstraintLayout>
